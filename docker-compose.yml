version: "3"
services:
    db:
        image: mysql:5.7.28
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: test
            MYSQL_USER: user
            MYSQL_PASSWORD: user
            TZ: 'Asia/Tokyo'
        volumes:
            - ./docker:/docker-entrypoint-initdb.d
